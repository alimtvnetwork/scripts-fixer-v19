{
  "install": {
    "method":      "curlOneLiner",
    "scriptName":  "gitmap",
    "releaseTag":  "main",
    "installUrl":  "https://raw.githubusercontent.com/alimtvnetwork/gitmap-v23/{tag}/install.sh",
    "binDir":      "${HOME}/.local/bin"
  },
  "verify": {
    "binary": "${HOME}/.local/bin/gitmap --version"
  }
}
